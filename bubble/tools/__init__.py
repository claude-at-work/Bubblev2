"""Verbs on top of bubble — composition over the substrate, not parallel
implementations of it. Each submodule is something an agent runtime asks
bubble to *do* for it (diff two versions, …) rather than something to load
through.
"""
